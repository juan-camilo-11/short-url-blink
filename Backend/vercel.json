{
    "builds": [
        {
            "src": "build/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "build/app.js"
        }
    ]
}