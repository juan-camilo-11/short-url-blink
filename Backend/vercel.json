{
    "builds": [
        {
            "src": "build/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "build/app.js"
        }
    ]
}